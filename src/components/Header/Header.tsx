import React from 'react'
import { StyledHeader } from './styled'
import { Logo } from '../Logo'

export const Header = () => {
  return (
    <StyledHeader>
        <Logo />
    </StyledHeader>
  )
}
