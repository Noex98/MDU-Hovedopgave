import {
    StyledSpinner,
    StyledContainer
} from './styled'

export const Spinner = () => {

    return (
        <StyledContainer>
            <StyledSpinner/>
        </StyledContainer>
    )
}
